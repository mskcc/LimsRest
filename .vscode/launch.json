{
    // Debug configuration for attaching to ./gradlew bootRun started in the terminal
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "request": "attach",
            "name": "Attach to Gradle BootRun (port 5005)",
            "hostName": "localhost",
            "port": 5005
        }
    ]
}